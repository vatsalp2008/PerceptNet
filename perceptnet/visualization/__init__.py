"""Visualization: 2D image overlays (OpenCV/matplotlib) and 3D point-cloud +
box rendering (Open3D, optional / offscreen-capable).

Imported explicitly by callers; Open3D is never required to import the package.
"""
