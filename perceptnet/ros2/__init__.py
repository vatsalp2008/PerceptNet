"""ROS 2 (Humble) interface. Requires rclpy + vision_msgs, which are available only
inside a sourced ROS 2 environment (Linux). All ROS imports live inside the node
module so this package never breaks ``import perceptnet`` off-platform.
"""
